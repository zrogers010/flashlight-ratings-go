# OS/editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Env and local secrets
.env
.env.*
worker.env
api.env
web.env
!.env.example
!.env.local.example
PROJECT_CONTEXT.md
*.pem
*.key
*.p12
*.pfx
*.crt
secrets/

# Logs and temp
*.log
tmp/
temp/
.cache/

# rainforest-sync sidecar state file (per-ASIN unavailable streaks).
# Regenerated locally on every `cmd/rainforest-sync` run; not consumed
# by the API, worker, deploy script, or DB import.
data/*.sync_state.json

# Test artifacts
web/mobile-screenshots/
web/mobile-test*.js

# Go
bin/
dist/
coverage.out
*.test
# stray local binaries from `go build -o NAME ./cmd/NAME`
/rainforest-sync
/amazon-sync
/catalog-build
/catalog-refresh

# Next.js / Node (web/)
web/node_modules/
web/.next/
web/out/
web/.turbo/
web/tsconfig.tsbuildinfo
web/.env.local
web/.env.development.local
web/.env.test.local
web/.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
